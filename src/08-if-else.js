// 1. Sentencia if

if (condition) {
    // código a ejecutar si la condición es verdadera
}

if (userAge >= 18) {
    console.log("Puedes ver el contenido restringido.");
}

// 2. Sentencia if-else

if (condition) {
    // código si es verdadero
} else {
    // código si es falso
}

if (userAge >= 18) {
    console.log("Puedes ver el contenido restringido.");
} else {
    console.log("Debes ser mayor de edad para ver este contenido.");
}

let score = 85;

if (score >= 90) {
    console.log("Excelente trabajo.");
} else if (score >= 70) {
    console.log("Aprobaste.");
} else {
    console.log("Necesitas repasar el material.");
}
